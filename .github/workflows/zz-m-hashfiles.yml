name: SONDA m – hashFiles nad priečinkom

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - name: nič
        run: echo ok
      - name: cache save so vzorom dem/**
        if: ${{ always() && hashFiles('dem/**') != '' }}
        uses: actions/cache/save@v4
        with:
          path: dem
          key: sonda-dem
